firebase-config.js
.DS_Store

# Config local de IA (clave de API) — nunca subir
ia-config.local.js
proxy-ia/.wrangler/
backups/
.wrangler/

# Cache local de Firebase Hosting
.firebase/

# Worktrees que crea Claude Code para trabajar aislado: son copias de ESTE
# mismo repo (rama diseno-premium, etc.), no codigo nuevo. Subirlas duplicaria
# el repo dentro de si mismo.
.claude/

# Capturas del medidor de móvil: material de trabajo, se regeneran con
# `node pruebas-movil.mjs` (CAPTURAS=1). Lo que se versiona es el medidor.
.capturas-movil/
